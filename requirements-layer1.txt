# Layer 1 (Foundation) runtime + test dependencies only.
#
# requirements.txt declares the full system's dependencies (all 4 layers).
# CI installs from THIS file instead, scoped to what Layer 1 actually imports,
# because several later-layer packages in requirements.txt are not yet
# Python 3.13 / numpy 2.x compatible -- see the note in requirements.txt.
# Update this file (and ci.yml) as each new layer's imports land.

# Streaming
confluent-kafka[avro]==2.5.0
fastavro==1.9.4

# Data
numpy==2.1.3

# Config
python-dotenv==1.0.1

# Utilities
structlog==24.2.0

# Dev / test
pytest==8.2.2
pytest-cov==5.0.0
pip-audit==2.7.3
