# Layer 2 (Intelligence) runtime + test dependencies.
# Extends layer 1. CI installs from this file starting with the Layer 2 build.
#
# Floor constraints (>=), not exact pins — same rationale as requirements-layer1.txt.
# Update ci.yml to reference this file when Layer 2 tests land.

# Streaming
confluent-kafka[avro,schemaregistry]>=2.15.0
fastavro>=1.12.2

# LLM — floor at 0.40.0 for stable tool_choice API; claude-sonnet-5 + strict tools verified on 0.84.0
anthropic>=1.2.0

# Data
numpy>=2.5.2
pandas>=3.0.5

# Config
python-dotenv>=1.2.3
pydantic>=2.13.5

# Utilities
structlog>=26.1.0
tenacity>=9.1.4

# Dev / test
pytest>=9.1.1
pytest-cov>=7.1.0
pip-audit>=2.10.1
