# Layer 4 (Feedback Loop) runtime + test dependencies.
# Extends layer 2. CI installs from this file starting with the Layer 4 build.
#
# great-expectations>=1.0.0 supports numpy 2.x (the 0.18.x series hard-pins
# numpy<2.0 which conflicts with Python 3.13 wheels). Layer 4 drift detection
# uses pure Python rolling stats; GE is documented as the production upgrade path.

# Streaming
confluent-kafka[avro,schemaregistry]>=2.15.0
fastavro>=1.12.2

# LLM
anthropic>=0.116.0

# Data
numpy>=2.5.1
pandas>=3.0.3

# Dashboard
streamlit>=1.59.2
plotly>=6.9.0

# Config
python-dotenv>=1.0.0
pydantic>=2.13.4

# Utilities
structlog>=26.1.0
tenacity>=9.1.4

# Dev / test
pytest>=9.1.1
pytest-cov>=7.1.0
pip-audit>=2.10.1
