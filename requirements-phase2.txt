# Phase 2 additional dependencies
# Core Phase 2 ML algorithms
scikit-learn>=1.9.0       # Platt scaling (LogisticRegression), DBSCAN, StandardScaler
scipy>=1.18.0             # cdist (cluster_analyzer), expit (sigmoid) available via sklearn.special
numpy>=2.5.1             # array operations (already in requirements.txt; listed for clarity)
