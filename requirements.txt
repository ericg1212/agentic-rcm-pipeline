# Full declared stack for the end-state system (all 4 layers). CI does NOT
# install from this file -- see requirements-layer1.txt for what's actually
# tested today. Known blocker for later layers: great-expectations==0.18.19
# hard-pins numpy<2.0, which conflicts with pandas/pyarrow/snowflake-connector
# needing numpy>=2.1 for Python 3.13 wheels. great-expectations 1.0+ supports
# numpy 2.x but is a breaking API rewrite -- resolve deliberately when the
# Quality layer is actually built, not as a side effect of a CI fix.

# Streaming
confluent-kafka[avro]==2.5.0
fastavro==1.9.4

# LLM
anthropic==0.31.0

# Data
pandas==2.2.3
numpy==2.1.3
pyarrow==23.0.1

# Snowflake
snowflake-connector-python[pandas]==3.13.1
snowflake-sqlalchemy==1.6.1

# Quality
great-expectations==0.18.19

# Dashboard
streamlit==1.54.0
plotly==5.22.0

# Config
python-dotenv==1.2.2
pydantic==2.9.2
pydantic-settings==2.5.2

# HTTP
requests==2.33.0
httpx==0.27.0

# Utilities
tenacity==8.4.1
structlog==24.2.0
uuid6==2024.1.12

# Dev / test
pytest==9.0.3
pytest-asyncio==0.23.7
pytest-cov==7.1.0
ruff==0.5.0
bandit==1.7.9
pip-audit==2.7.3
