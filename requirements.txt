# Full declared stack for the end-state system (all 4 layers). CI does NOT
# install from this file -- see requirements-layer1.txt for what's actually
# tested today. Known blocker for later layers: great-expectations==0.18.22
# hard-pins numpy<2.0, which conflicts with pandas/pyarrow/snowflake-connector
# needing numpy>=2.1 for Python 3.13 wheels. great-expectations 1.0+ supports
# numpy 2.x but is a breaking API rewrite -- resolve deliberately when the
# Quality layer is actually built, not as a side effect of a CI fix.

# Streaming
confluent-kafka[avro,schemaregistry]==2.15.0
fastavro==1.12.2

# LLM
anthropic==0.116.0

# Data
pandas==3.0.3
numpy==2.5.1
pyarrow==25.0.0

# Snowflake
snowflake-connector-python[pandas]==4.7.1
snowflake-sqlalchemy==1.11.0

# Quality
great-expectations==0.18.22

# Config
python-dotenv==1.2.2
pydantic==2.13.4
pydantic-settings==2.14.2

# HTTP
requests==2.34.2
httpx==0.28.1

# Utilities
tenacity==9.1.4
structlog==26.1.0
uuid6==2025.0.1

# Dev / test
pytest==9.1.1
pytest-asyncio==1.4.0
pytest-cov==7.1.0
ruff==0.15.21
bandit==1.9.4
pip-audit==2.10.1
